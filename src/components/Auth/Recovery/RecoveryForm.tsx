import { Flex, Grid } from "@wipsie/ui";

export const RecoveryForm = () => {
  return (
    <Flex fullWidth p={2} pt={2}>
      <Grid container spacing={1}>
        <Grid item xs={12}></Grid>
      </Grid>
    </Flex>
  );
};
